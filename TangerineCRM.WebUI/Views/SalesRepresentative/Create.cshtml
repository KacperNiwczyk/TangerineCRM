@model TangerineCRM.WebUI.Models.SalesRepresentativeViewModel
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@Styles.Render("~/Content/login")
@Styles.Render("~/Content/dropdown")

<h2>Create</h2>
<body>
    <div class="wrapper fadeInDown" style="align-items: flex-start">
        <div id="formContent">
            @using (Html.BeginForm("Add", "SalesRepresentative", FormMethod.Post))
            {
                @Html.TextBoxFor(x => x.SingleRepresentative.FirstName, new { @class = "fadeIn second", @name = "Imię", @placeholder = "Imię" });
                //@Html.ValidationMessageFor();
                @Html.TextBoxFor(x => x.SingleRepresentative.LastName, new { @class = "fadeIn second", @name = "Nazwisko", @placeholder = "Nazwisko" });
                //@Html.ValidationMessageFor();
                <input type="submit" class="fadeIn fourth" value="Dodaj">
            }
            <!-- Remind Passowrd -->
        </div>
    </div>
</body>